# 📋 需要上传到GitHub的文件清单

## 🎯 构建APK必需的文件

### 核心文件（必须上传）

1. **`main.py`** ✅
   - Android应用主文件
   - 包含所有应用逻辑

2. **`buildozer.spec`** ✅
   - Buildozer构建配置文件
   - 定义应用信息、依赖、权限等

3. **`.github/workflows/build_apk.yml`** ✅
   - GitHub Actions工作流配置
   - 用于自动构建APK

---

## 📁 文件详细说明

### 必需文件（3个）

```
项目根目录/
├── main.py                          # Android应用主文件
├── buildozer.spec                   # 构建配置文件
└── .github/
    └── workflows/
        └── build_apk.yml            # GitHub Actions配置
```

---

## 🚀 手动上传步骤

### 步骤1：在GitHub创建仓库

1. 访问：https://github.com/new
2. 输入仓库名（例如：`ssh-tool-android`）
3. 选择 Public
4. 点击 "Create repository"

### 步骤2：准备文件

确保以下文件在项目目录中：

- ✅ `main.py`
- ✅ `buildozer.spec`
- ✅ `.github/workflows/build_apk.yml`（如果使用在线构建）

### 步骤3：上传文件

**方法A：使用GitHub网页上传**

1. 进入创建的仓库页面
2. 点击 "uploading an existing file"
3. 拖拽或选择以下文件：
   - `main.py`
   - `buildozer.spec`
   - `.github/workflows/build_apk.yml`（需要先创建目录结构）

**方法B：使用Git命令**

```bash
# 1. 初始化Git
git init

# 2. 添加文件
git add main.py
git add buildozer.spec
git add .github/workflows/build_apk.yml

# 3. 提交
git commit -m "初始提交：添加APK构建文件"

# 4. 添加远程仓库
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 5. 上传
git push -u origin main
```

---

## 📝 文件检查清单

在上传前，请确认：

- [ ] `main.py` 存在且完整
- [ ] `buildozer.spec` 存在且配置正确
- [ ] `.github/workflows/build_apk.yml` 存在（如果使用在线构建）
- [ ] 所有文件在同一目录下

---

## ⚠️ 注意事项

### 不需要上传的文件

以下文件**不需要**上传：
- ❌ `*.bat` - Windows批处理文件
- ❌ `*.md` - 说明文档（可选）
- ❌ `*.exe` - 可执行文件
- ❌ `build/` - 构建输出目录
- ❌ `dist/` - 分发目录
- ❌ `__pycache__/` - Python缓存
- ❌ `*.pyc` - 编译的Python文件
- ❌ `*.json.backup` - 备份文件
- ❌ `*.log` - 日志文件

### 可选文件（不影响构建）

以下文件可以上传，但不是必需的：
- `README.md` - 项目说明（推荐）
- `requirements.txt` - Python依赖（如果有）
- `requirements_android.txt` - Android依赖（如果有）

---

## 🎯 最简上传方案

**最少只需要3个文件：**

1. `main.py`
2. `buildozer.spec`
3. `.github/workflows/build_apk.yml`

这三个文件就足够构建APK了！

---

## ✅ 快速检查

运行以下命令检查文件是否存在：

```bash
# Windows命令提示符
dir main.py
dir buildozer.spec
dir .github\workflows\build_apk.yml
```

如果都能找到，就可以上传了！

